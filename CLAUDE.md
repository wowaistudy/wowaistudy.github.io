# wowaistudy 블로그 작업 지침

이 저장소는 GitHub Pages + Jekyll(Reverie 테마) 기반의 공부 기록 블로그다.
주소: https://wowaistudy.github.io  (GitHub Organization `wowaistudy` 소유)

## 익명성 규칙 (가장 중요)

- 이 블로그는 필명 `wowaistudy`로 운영한다. 운영자의 실명, 실명 GitHub 계정, 개인 이메일을 파일, 커밋, 글 어디에도 넣지 않는다.
- 커밋 저자는 저장소 로컬 설정(user.name=wowaistudy, user.email=wowaistudy@users.noreply.github.com)을 그대로 쓴다. 전역 설정으로 덮어쓰지 않는다.
- 글은 회사에서 공부한 내용을 공개적으로 올리는 것이다. 다음은 반드시 제거하거나 일반화한다.
  - 회사명, 부서명, 동료 이름, 고객사와 파트너사 이름
  - 내부 시스템, 프로젝트, 서비스의 고유 이름
  - 실제 수치(매출, 사용자 수, 성능 지표 등)와 사내 코드, 내부 URL
  - 아직 공개되지 않은 계획이나 일정
- 위 항목이 초안에 섞여 있으면 그대로 올리지 말고 사용자에게 알린 뒤 일반화한다.

## 매일 글 올리는 흐름

1. 사용자가 그날 공부한 내용을 메모나 말로 전달한다. 정리되지 않은 상태여도 된다.
2. `_posts/YYYY-MM-DD-영문-슬러그.md` 로 초안을 만든다. 날짜는 오늘 날짜(로컬 기준).
3. 사용자가 읽고 확인한 뒤에만 커밋하고 푸시한다. 사용자의 확인 없이 푸시하지 않는다.
4. 커밋 메시지는 짧게 한국어 또는 영어로 쓴다. 예: `Add post: 오늘 공부한 RAG 개념`
5. 푸시 후 1~2분이면 GitHub Pages가 자동으로 빌드한다.

## 글 형식

```yaml
---
layout: post
title: 글 제목
categories: [공부 기록]
---
```

- 카테고리는 당분간 `공부 기록` 하나만 쓴다. 필요해지면 사용자와 상의해 추가한다.
- 글 길이는 짧아도 된다. 하루 기록이 목적이므로 완성도보다 꾸준함을 우선한다.
- 사용자의 문체를 살린다. 문장을 매끈한 AI 문체로 다듬지 않는다. 글쓰기 피드백 기준은 `writing-agent.md`를 따른다.
- 코드는 펜스 코드블록으로, 링크는 공개된 자료만 넣는다.

## 하지 말 것

- 디자인, 레이아웃, 테마 파일(`_layouts`, `_includes`, `_sass`, `assets`)은 사용자가 요청할 때만 수정한다.
- `_config.yml`은 사용자가 요청할 때만 수정한다.
- 글을 대신 지어내지 않는다. 사용자가 전달한 내용 범위 안에서만 쓴다.

## 로컬 미리보기 (선택)

```bash
bundle install
bundle exec jekyll serve
```
